# App/database
DATABASE_URL="postgresql://woonpoints:woonpoints@db:5432/woonpoints?schema=public"

# Safety: current baseline must remain dry-run only
BOT_DRY_RUN=true

# App identity
NEXT_PUBLIC_APP_NAME="Woonpoints"
NEXT_PUBLIC_REGION="Amsterdam"

# Future integrations — leave empty until configured
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
EMAIL_FROM=
WHATSAPP_PROVIDER=
WHATSAPP_API_KEY=
